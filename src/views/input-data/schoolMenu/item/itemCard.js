import React from 'react';

// material-ui
import { useTheme } from '@mui/material/styles';
import {
    Grid,
    Table,
    TableBody,
    TableCell,
    TableContainer,
    TableRow,
    Typography,
} from '@mui/material';

const ItemCard = ({ menus }) => {
    const theme = useTheme();

    return (
        <h1>Hello</h1>
    );
};

export default ItemCard;